version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

python:
  install:
    - pip install ipykernel
    - pip install -r requirements.txt
    - python -m ipykernel install --user --name new-env --display-name "Python (new-env)"

sphinx:
  configuration: conf.py